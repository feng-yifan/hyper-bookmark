import React from 'react';
import ReactDOM from 'react-dom/client';
import './main.css';
import Popup from '@pages/Popup/index.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Popup/>
  </React.StrictMode>,
);
